import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.gamebilling',
  appName: 'Game Billing',
  webDir: 'dist'
};

export default config;
